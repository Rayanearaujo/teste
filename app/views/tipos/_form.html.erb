<%= simple_form_for(@tipo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :descricao %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Salvar'%>
  </div>
<% end %>
